prt.hrl - version 1.0
==============

erlang based utility to track pages printed on a printer by
scraping page total from printer 

Requirements
==============
1. erlang
2. postgresql
3. http://code.google.com/p/erlang-psql-driver/
4. create prt.hrl with this information filled out properly:

-define(MAGIC_LINE_NUMBER, 208).
-define(URL, "").
-define(HOST, "localhost").
-define(DB, "").
-define(USERNAME, "").
-define(PASSWORD, "").
